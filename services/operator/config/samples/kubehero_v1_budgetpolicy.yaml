apiVersion: kubehero.kubehero.io/v1
kind: BudgetPolicy
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: budgetpolicy-sample
spec:
  # TODO(user): Add fields here
